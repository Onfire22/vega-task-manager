services:
  vega-database:
    image: postgres:18-alpine
    container_name: vega-database
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "5432:5432"
    volumes:
      - vega-database:/var/lib/postgresql

volumes:
  vega-database:
    name: vega-database