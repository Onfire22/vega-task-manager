model ChatMessages {
  uuid String @id @default(uuid())

  text        String
  isPinned    Boolean       @default(false)
  isSystem    Boolean       @default(false)
  channelUuid String
  channel     ChatChannels  @relation(fields: [channelUuid], references: [uuid])
  authorUuid  String
  author      User          @relation(fields: [authorUuid], references: [uuid])
  replyToUuid String?
  replyTo     ChatMessages? @relation("MessageReply", fields: [replyToUuid], references: [uuid])

  replies ChatMessages[] @relation("MessageReply")

  createdAt DateTime  @default(now()) @map("created_at")
  updatedAt DateTime  @updatedAt @map("updated_at")
  deletedAt DateTime?

  @@map("chat_messages")
}
