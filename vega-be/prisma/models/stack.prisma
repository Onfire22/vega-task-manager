model Stack {
  id       String @id @default(uuid())
  name     String @unique
  fullName String @unique @map("full_name")
  color    String @unique

  users User[]
  tasks Task[]

  @@map("stacks")
}
